name: GenText Workflow

on:
  push:
    branches:
      - main

jobs:
  generate_text:
    name: Generate Text (Windows)
    runs-on: windows-latest
    environment:
      name: gentext-launch

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.10

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Run tests
      run: pytest

  generate_text:
    name: gentext
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.10

    - name: Install Dependencies
      run: |
        pip install gentext  
      shell: bash

    - name: Generate Text
      run: |
        gentext.py  
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  
